linters:
  csslint: {  }
  shellcheck:
    shell: bash
  flake8:
    fixer: true
  rubocop:
    fixer: true
  phpcs:
    standard: PSR2
    fixer: true
files:
  ignore:
    - 'bower_components/*'
    - 'node_modules/*'
    - 'vendor/*'
fixers:
  enable: true
